server:
  port: 8080
  https:
    cert:   # /ssl/tls.crt
    key:    # /ssl/tls.key
  corsorigins:
    - http://localhost:3000
  env: os
database:
  username: postgres
  password: password
  host: localhost
  port: 5432
  name: mgmt
  version: 1
  timezone: Etc/UTC
  pool:
    idleconnections: 5
    maxconnections: 10
    connlifetime: 30 # In minutes
usagebackend:
  host: localhost
  port: 8084
  https:
    cert:  # /ssl/tls.crt
    key:  # /ssl/tls.crt
